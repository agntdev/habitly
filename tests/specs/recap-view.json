[
  {
    "name": "Weekly recap — empty state",
    "steps": [
      {
        "send": { "callback": "recap:view" },
        "expect": [
          {
            "method": "answerCallbackQuery"
          },
          {
            "method": "editMessageText",
            "payload": {
              "text": "No habits to recap yet — create one first!"
            }
          }
        ]
      }
    ]
  }
]
